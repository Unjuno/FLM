# FLM - セキュリティチェック
# 定期的な依存関係の脆弱性スキャン

name: Security Check

on:
  push:
    branches: [ main, develop ]
    paths-ignore:
      - 'archive/**'  # アーカイブの変更はセキュリティチェックをスキップ
  pull_request:
    branches: [ main, develop ]
    paths-ignore:
      - 'archive/**'
  schedule:
    - cron: '0 0 * * 0' # 毎週日曜日の午前0時（UTC）

jobs:
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package.json'
      
      - name: Install dependencies
        run: npm ci
        if: ${{ hashFiles('package.json') != '' }}
        continue-on-error: true
      
      - name: Run npm audit
        run: npm audit --audit-level=high
        if: ${{ hashFiles('package.json') != '' }}
        continue-on-error: false
      
      - name: Run npm audit (moderate and low)
        run: npm audit --audit-level=moderate
        if: ${{ hashFiles('package.json') != '' }}
        continue-on-error: true
      
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo-audit
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: ${{ runner.os }}-cargo-audit-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-audit-
      
      - name: Install cargo-audit
        run: |
          if ! command -v cargo-audit &> /dev/null; then
            cargo install cargo-audit --locked
          fi
      
      - name: Run cargo audit (excluding archive)
        run: |
          # アーカイブを除外してセキュリティチェック
          if [ -d "src-tauri" ] && [ ! -d "src-tauri/archive" ]; then
            cargo audit --manifest-path src-tauri/Cargo.toml || true
          fi
          if [ -d "crates" ]; then
            find crates -name "Cargo.toml" -exec cargo audit --manifest-path {} \; || true
          fi
        continue-on-error: true
      
      - name: Run security tests
        run: npm run test:security
        if: ${{ hashFiles('package.json') != '' }}
        continue-on-error: true
