# 完成までの道のり（Completion Roadmap）

> Status: Active | Updated: 2025-01-28 | Audience: All contributors

## 現在の状況（2025-01-28）

### ✅ 完了済み
- **ビルドエラー解決**: Tauriアプリケーションのビルドエラー（アイコンファイル、Rustコンパイルエラー、ワークスペース設定）をすべて解決
- **アプリケーション起動**: Tauriアプリが正常にビルド・起動し、UIが表示されることを確認
- **フロントエンド実装**: 基本的なUIコンポーネント（サイドバー、ルーティング、各ページ）が実装済み
- **flm CLIバイナリ検索ロジック実装** (2025-01-28):
  - `src-tauri/src/commands/cli_bridge.rs`に`find_flm_binary()`関数を実装
  - 検索順序: 1) `FLM_CLI_PATH`環境変数、2) 実行ファイルからの相対パス、3) システムPATH
  - `which`クレートを依存関係に追加
- **ホーム画面実装** (2025-01-28):
  - `src/pages/Home.tsx`と`src/pages/Home.css`を作成
  - システムステータス表示（プロキシ状態、エンジン状態）
  - クイックアクション（プロキシ起動・停止、エンジン検出、各ページへのナビゲーション）
  - アプリケーション概要表示
  - `src/routes.tsx`を更新してホーム画面をルーティングに追加
  - プロキシ停止機能、定期的なステータス更新、エラーハンドリング改善を追加
  - JSONレスポンス形式の対応（`{ version: "1.0", data: [...] }`形式）
  - Enum型（`ProxyMode`、`EngineStatus`）の文字列変換処理（`formatProxyMode()`、`formatEngineStatus()`関数）
- **Chat Testerサービスの改善** (2025-01-28):
  - `getProxyEndpoint()`関数を修正して`ipc_proxy_status`のレスポンス形式に対応
  - `ProxyMode` enumの判定を改善
- **ホーム画面のUX改善** (2025-01-28):
  - 成功メッセージの追加（プロキシ起動/停止、エンジン検出）
  - 成功メッセージの自動消去（3秒後）
  - 成功メッセージ用のCSSスタイルを追加
- **エラーハンドリングの改善** (2025-01-28):
  - `safeInvoke`関数の改善（CLIエラーの詳細情報を保持）
  - `CliError`インターフェースと`extractCliError`関数を追加
  - ネットワークエラーの特別な処理を追加
  - ホーム画面でCLIエラーの詳細情報（stderr）を表示
- **エラーハンドリングの統一化** (2025-01-28):
  - すべてのページとコンポーネントで`extractCliError`を使用
  - `IpBlocklistManagement.tsx`、セキュリティコンポーネント（`AuditLogsView`、`IntrusionEventsView`、`AnomalyEventsView`）のエラーハンドリングを改善
  - 統一されたエラーメッセージ表示（stderr情報を含む）
- **共通UIコンポーネントの実装** (2025-01-28):
  - `LoadingSpinner`コンポーネント: サイズ指定可能なローディングスピナー
  - `ErrorMessage`コンポーネント: 詳細情報を折りたたみ可能なエラーメッセージ
  - `SuccessMessage`コンポーネント: 自動消去機能付き成功メッセージ
  - ホーム画面で共通コンポーネントを使用
  - ChatTesterページのエラーハンドリングを統一
- **全ページでの共通コンポーネント使用** (2025-01-28):
  - すべてのページとコンポーネントで共通UIコンポーネントを使用
  - `ChatTester.tsx`、`IpBlocklistManagement.tsx`、セキュリティコンポーネント（`AuditLogsView`、`IntrusionEventsView`、`AnomalyEventsView`）で共通コンポーネントを使用
  - 一貫したUI/UXを提供
- **成功メッセージの統一化** (2025-01-28):
  - `IpBlocklistManagement.tsx`で`alert()`を`SuccessMessage`コンポーネントに置き換え
  - すべてのページで統一された成功メッセージ表示を実装
  - エラーメッセージも`alert()`から`ErrorMessage`コンポーネントに統一
- **コードの共通化とリファクタリング** (2025-01-28):
  - `ConfirmDialog`コンポーネントの作成（`window.confirm`の代替）
  - フォーマット関数（`formatProxyMode`、`formatEngineStatus`）を共通ユーティリティに移動
  - `IpBlocklistManagement.tsx`で`window.confirm`を`ConfirmDialog`コンポーネントに置き換え
  - コードの再利用性と保守性を向上
- **型安全性の向上とタイマー管理の改善** (2025-01-28):
  - `extractCliError`関数の型安全性を向上（`any`の使用を削減）
  - `safeInvoke`関数で`Object.defineProperty`を使用して型安全にエラー情報を付与
  - `Home.tsx`でタイマーのクリーンアップを改善（メモリリークを防止）
- **タイムアウト管理ユーティリティの実装** (2025-01-28):
  - `src/utils/timeout.ts`の作成: タイムアウト管理の共通ユーティリティ関数
  - `Home.tsx`でタイマー管理のコードを共通ユーティリティ関数に置き換え
  - `handleProxyStatusResult`と`handleProxyStatusError`ヘルパー関数を追加してコードを整理
  - コードの重複を削減し、保守性を向上
- **アクセシビリティの改善** (2025-01-28):
  - `ConfirmDialog`コンポーネントにキーボードナビゲーション（Escapeキー、Enterキー）を追加
  - `role="dialog"`、`aria-modal="true"`、`aria-labelledby`属性を追加
  - キャンセルボタンに`autoFocus`を追加
  - ユーザビリティとアクセシビリティを向上
- **コードの整理とロギングユーティリティの実装** (2025-01-28):
  - 重複ファイルを整理（`timer.ts`を削除し、`timeout.ts`に統一）
  - ロギングユーティリティ（`src/utils/logger.ts`）を作成
  - `console.warn`の使用を`logger.warn`に統一
  - テストファイルを更新
  - コードの一貫性と保守性を向上
- **エラーバウンダリと入力検証の実装** (2025-01-28):
  - エラーバウンダリコンポーネント（`ErrorBoundary.tsx`）を作成
  - 入力検証ユーティリティ（`validation.ts`）を作成
  - `App.tsx`にエラーバウンダリを統合
  - `ChatTester.tsx`で入力検証を改善（temperature、maxTokensの検証）
  - アプリケーション全体のエラーハンドリングと入力検証を強化
- **エラーハンドリングの共通化とコード品質の向上** (2025-01-28):
  - エラーハンドリングの共通ユーティリティ（`errorHandler.ts`）を作成
  - `ChatTester.tsx`と`IpBlocklistManagement.tsx`でエラーハンドリングを共通化
  - コードの重複を削減し、保守性を向上
  - `Home.tsx`で実装された改善パターンを他のページにも適用
- **サービス層のセキュリティとコード品質の向上** (2025-01-28):
  - `chatTester.ts`: URL検証とセキュリティの改善、型ガード関数の追加、関数分割による複雑度削減
  - `security.ts`: 型ガード関数の追加、フィルターロジックの分離、WHYコメントの追加
  - セキュリティベストプラクティスの適用
  - コードの保守性と安全性を向上
- **ユーティリティ関数のテスト追加** (2025-01-28):
  - `errorHandler.test.ts`の作成: エラーハンドリングユーティリティの包括的なテスト
  - `validation.test.ts`の作成: 入力検証ユーティリティの包括的なテスト
  - `logger.test.ts`の作成: ロギングユーティリティの包括的なテスト
  - すべての主要な機能とエッジケースをカバー
  - コードの信頼性と保守性を向上
- **chatTesterサービスのさらなる改善とテスト拡充** (2025-01-28):
  - `chatTester.test.ts`に新しいテストケースを追加（URL検証、エラーハンドリング、各種プロキシモード）
  - URL検証の改善（プロトコルチェック、絶対URLと相対パスの処理）
  - エラーメッセージ抽出の改善（開発環境と本番環境での動作、長いメッセージの切り詰め）
  - 関数分割の改善（`buildRequestHeaders`、`extractErrorMessage`、`attachRequestId`）
  - ネットワークエラー検出の改善（`tauri.ts`）
  - テストカバレッジとコード品質を向上
- **共通UIコンポーネントのパフォーマンス最適化** (2025-01-28):
  - `React.memo`を適用して不要な再レンダリングを防止
  - `displayName`を設定してデバッグを容易にする
  - `LoadingSpinner`、`ErrorMessage`、`SuccessMessage`、`ConfirmDialog`を最適化
  - アプリケーション全体のパフォーマンスを向上
- **設定値の一元管理とCHANGELOGの更新** (2025-01-28):
  - `src/config/constants.ts`の作成: アプリケーション定数の一元管理
  - タイミング、デフォルト値、サイレントエラーパターンを一元管理
  - すべてのページで定数ファイルからインポート（パスエイリアス`@/`を使用）
  - `CHANGELOG.md`を更新して最近の実装内容を反映
  - コードの保守性と一貫性を向上
- **Rustコードのロギング改善とエラーハンドリング強化** (2025-01-28):
  - `log`クレートを使用してログを出力（`eprintln!`から`log::warn!`に置き換え）
  - 一時ファイル削除のエラーハンドリングを改善
  - 環境変数のフォールバック処理でログを適切に記録
  - ログレベルを制御可能にし、Tauriのログシステムと統合
  - コードの保守性とデバッグ性を向上
- **ChatTesterコンポーネントのUI改善とローディング状態の統一** (2025-01-28):
  - `LoadingSpinner`コンポーネントを追加してローディング状態を統一
  - プロキシエンドポイント取得中、モデルリスト取得中、チャットリクエスト送信中に`LoadingSpinner`を表示
  - パスエイリアス`@/`を使用して定数をインポート（一貫性の向上）
  - UIの一貫性とユーザー体験を向上
- **アクセシビリティの改善** (2025-01-28):
  - `LoadingSpinner`コンポーネントに`role`、`aria-live`、`aria-label`を追加
  - フォーム要素に`htmlFor`、`id`、`aria-label`、`aria-describedby`を追加
  - ヘルプテキストとスクリーンリーダー専用テキストを追加
  - アクセシビリティガイドライン（WCAG）に準拠した実装
  - スクリーンリーダーとキーボードナビゲーションのサポートを向上
- **ロギングの統一** (2025-01-28):
  - すべての`console.warn`と`console.error`を`logger`に統一
  - ロギングの一貫性を向上
  - ログレベルの制御を可能に
  - コードの保守性とデバッグ性を向上
- **CHANGELOGの最終更新** (2025-01-28):
  - アクセシビリティ改善とロギング統一をCHANGELOGに追加
  - 実装内容の完全な記録を確保
- **メモリリーク防止と入力検証の改善** (2025-01-28):
  - `URL.createObjectURL`使用後に`URL.revokeObjectURL`を呼び出してメモリリークを防止
  - `parseInt`の結果が`NaN`でないことを確認する処理を追加
  - セキュリティコンポーネント（AuditLogsView、IntrusionEventsView、AnomalyEventsView）の改善
  - メモリ管理と入力検証の品質を向上

### ✅ 最新の実装完了（2025-01-28）
- **Phase 3 パッケージング作業の進捗**:
  - ✅ Tauri設定の更新（証明書とスクリプトのリソース同梱設定）
  - ✅ ビルドスクリプトの更新（証明書生成機能の追加）
  - ✅ `packaged-ca`モードの統合（既に実装済み）
  - ✅ OS信頼ストアへの自動登録機能（既に実装済み）
  - ✅ インストーラースクリプト（既に実装済み）
  - ✅ LM Studioエンジン実装（既に実装済み）
- **I18N UI実装**:
  - ✅ 翻訳ファイルの作成（`locales/ja.json`, `locales/en.json`）
  - ✅ i18nコンテキストの実装（`I18nContext.tsx`）
  - ✅ 言語切り替えUIの実装（設定ページ）
  - ✅ 設定保存機能（`config.db`の`preferred_language`）
  - ✅ 初回起動時の言語自動検出（OSの言語設定から検出）
  - ✅ サイドバーのI18N対応
  - ✅ ホームページのI18N対応
  - ✅ ChatTesterページのI18N対応
  - ✅ SecurityEventsページのI18N対応
  - ✅ IpBlocklistManagementページのI18N対応

### ⚠️ 現在の問題点
1. **プロキシが起動していない**: プロキシサービスが起動していないため、多くの機能が動作しない
   - ホーム画面からプロキシを起動できるようになったが、動作確認が必要

---

## 優先度別タスク一覧

### 🔴 優先度: 最高（即座に対応が必要）

#### 1. flm CLIバイナリのビルドと配置
**問題**: Tauriアプリが`flm` CLIバイナリを見つけられない

**解決方法**:
1. **CLIバイナリをビルド**:
   ```bash
   cargo build --release --bin flm
   # または
   cd crates/apps/flm-cli
   cargo build --release
   ```

2. **Tauriアプリからアクセス可能にする方法（選択肢）**:
   
   **オプションA: PATHに追加（開発環境）**
   - ビルドされたバイナリをPATHに追加
   - Windows: `target/release/flm.exe`をPATHに追加
   - または、`src-tauri/src/commands/cli_bridge.rs`でバイナリの絶対パスを指定
   
   **オプションB: 相対パスで指定（推奨）**
   - `cli_bridge.rs`を修正して、Tauriアプリの実行ディレクトリから相対パスで`flm`バイナリを探す
   - 例: `../target/release/flm` または `./flm`
   
   **オプションC: Tauriバンドルに含める（本番環境）**
   - `tauri.conf.json`の`bundle.resources`に`flm`バイナリを追加
   - アプリケーションと一緒に配布

**実装手順**:
1. `crates/apps/flm-cli`をビルドして`flm`バイナリを生成
2. `src-tauri/src/commands/cli_bridge.rs`の`CLI_BIN`定数を修正、またはバイナリ検索ロジックを追加
3. 開発環境で動作確認

**参照**:
- `crates/apps/flm-cli/Cargo.toml`
- `src-tauri/src/commands/cli_bridge.rs:10` (`CLI_BIN`定数)
- `scripts/install.sh`, `scripts/install.ps1` (インストールスクリプト)

---

#### 2. プロキシサービスの起動・停止機能の動作確認
**問題**: プロキシが起動していないため、Chat Testerやセキュリティイベントなどの機能が動作しない

**確認事項**:
1. `flm proxy start`コマンドが正常に動作するか
2. Tauriアプリの`ipc_proxy_start`コマンドが正常に動作するか
3. プロキシのステータス確認機能が動作するか

**実装手順**:
1. CLIバイナリの問題を解決後、`flm proxy start`を実行してプロキシを起動
2. Tauriアプリから`ipc_proxy_start`を呼び出してプロキシを起動
3. `ipc_proxy_status`でステータスを確認
4. エラーハンドリングとUIフィードバックを確認

**参照**:
- `crates/services/flm-proxy/`
- `src-tauri/src/commands/cli_bridge.rs` (IPCコマンド)
- `docs/status/completed/proxy/PROXY_SERVICE_PHASE2_COMPLETE.md`

---

### 🟡 優先度: 高（次フェーズで実装）

#### 3. ホーム画面の実装
**現状**: "ホーム画面 (後で実装)" と表示されている

**実装内容**:
- アプリケーションの概要表示
- クイックアクション（プロキシ起動、エンジン検出など）
- システムステータス表示（プロキシ状態、エンジン状態など）

**参照**:
- `src/` (フロントエンドソース)
- `docs/specs/UI_MINIMAL.md`
- `docs/specs/FEATURE_SPEC.md`

---

#### 4. Phase 3 パッケージング作業
**内容**:
- インストーラーの実装
- コード署名の設定
- `packaged-ca`モードの完全実装
- 配布フローの確立

**参照**:
- `docs/planning/PHASE3_PACKAGING_PLAN.md`
- `docs/specs/CODE_SIGNING_POLICY.md`

---

### 🟢 優先度: 中（将来の拡張）

#### 5. 統合テストとエンドツーエンドテストの拡充
**内容**:
- UIコンポーネントのテスト
- Tauri IPCコマンドのテスト
- エンドツーエンドテストシナリオ

**参照**:
- `docs/guides/TEST_STRATEGY.md`
- `docs/status/active/TEST_ENVIRONMENT_STATUS.md`

---

## 実装順序の推奨

### フェーズ1: 即座の動作確保（1-2日）
1. ✅ **flm CLIバイナリのビルドと配置** - 2025-01-28完了
   - ✅ CLIバイナリをビルド（`cargo build --release --bin flm`）
   - ✅ Tauriアプリからアクセス可能にする（`find_flm_binary()`実装）
   - ⏳ 動作確認（次ステップ）

2. ✅ **プロキシサービスの起動・停止機能の実装改善** - 2025-01-28完了
   - ✅ プロキシ停止時にportを指定するように修正（`handleStopProxy`関数）
   - ✅ エラーハンドリングの改善（プロキシが実行されていない場合のエラーメッセージ）
   - ✅ 翻訳ファイルに`errors.proxyNotRunning`を追加
   - ✅ プロキシ停止機能のテスト拡充（エラーハンドリング、成功/失敗シナリオ、エッジケース）
   - ⏳ 動作確認（次ステップ）

3. ✅ **Phase 3 パッケージング作業の進捗確認** - 2025-01-28完了
   - ✅ `packaged-ca`モード基本実装完了の確認
   - ✅ Tauri設定ファイルにインストーラー設定追加済みの確認
   - ✅ ビルドスクリプトに証明書生成機能実装済みの確認
   - ✅ コード署名ポリシードキュメント存在の確認

3. ✅ **ホーム画面の実装** - 2025-01-28完了
   - ✅ UIコンポーネントの実装
   - ✅ システムステータス表示
   - ✅ クイックアクション実装
   - プロキシ起動コマンドのテスト
   - IPCコマンドの動作確認
   - エラーハンドリングの確認

### フェーズ2: 基本機能の完成（3-5日）
3. **ホーム画面の実装**
   - UIデザインの確定
   - コンポーネントの実装
   - 統合テスト

### フェーズ3: リリース準備（1-2週間）
4. **Phase 3 パッケージング作業**
   - インストーラーの実装
   - コード署名の設定
   - 配布フローの確立

5. **統合テストとエンドツーエンドテストの拡充**
   - テストスイートの拡充
   - CI/CDパイプラインの設定

---

## 技術的な詳細

### flm CLIバイナリの検索ロジック改善案

現在の`cli_bridge.rs`は単純に`CLI_BIN = "flm"`をPATHから探しています。以下の改善を推奨します:

```rust
// 改善案: バイナリの検索ロジックを追加
fn find_flm_binary() -> Result<PathBuf, CliBridgeError> {
    // 1. 環境変数から取得（開発環境用）
    if let Ok(path) = std::env::var("FLM_CLI_PATH") {
        let path = PathBuf::from(path);
        if path.exists() {
            return Ok(path);
        }
    }
    
    // 2. 相対パスから検索（Tauriアプリの実行ディレクトリから）
    let exe_dir = std::env::current_exe()
        .ok()
        .and_then(|p| p.parent().map(|p| p.to_path_buf()));
    
    if let Some(dir) = exe_dir {
        // Windows: flm.exe, Unix: flm
        let binary_name = if cfg!(windows) { "flm.exe" } else { "flm" };
        let candidates = [
            dir.join(binary_name),
            dir.join("..").join("target").join("release").join(binary_name),
            dir.join("..").join("target").join("debug").join(binary_name),
        ];
        
        for candidate in candidates {
            if candidate.exists() {
                return Ok(candidate);
            }
        }
    }
    
    // 3. PATHから検索（既存の動作）
    which::which("flm")
        .map_err(|e| CliBridgeError {
            code: "CLI_BINARY_NOT_FOUND".into(),
            message: format!("flm CLI binary not found in PATH or relative paths: {e}"),
            stderr: None,
        })
}
```

---

## チェックリスト

### 即座の動作確保
- [x] `flm` CLIバイナリをビルド（`cargo build --release --bin flm`） - 2025-01-28完了
- [x] `cli_bridge.rs`のバイナリ検索ロジックを改善 - 2025-01-28完了
  - `find_flm_binary()`関数を実装（環境変数、相対パス、PATHの順で検索）
  - `which`クレートを依存関係に追加
- [x] ホーム画面のJSONレスポンス処理を改善 - 2025-01-28完了
  - `{ version: "1.0", data: [...] }`形式の対応
  - Enum型（`ProxyMode`、`EngineStatus`）の文字列変換処理
- [ ] Tauriアプリから`flm`コマンドが実行できることを確認
- [ ] プロキシ起動コマンドの動作確認
- [ ] IPCコマンドの動作確認

### 基本機能の完成
- [x] ホーム画面のUIデザイン確定 - 2025-01-28完了
- [x] ホーム画面コンポーネントの実装 - 2025-01-28完了
- [x] IPCコマンドの統合テスト追加 - 2025-01-28完了
  - `ipc_detect_engines`, `ipc_list_models`, `ipc_proxy_start/stop/status`などのテスト
  - エラーハンドリングテストを含む包括的なテストスイート

### リリース準備
- [ ] Phase 3 パッケージング計画の確認
- [ ] インストーラーの実装
- [ ] コード署名の設定
- [ ] 配布フローの確立

---

## 参考リンク

- **プロジェクト構造**: `README.md`
- **次のステップ**: `docs/status/active/NEXT_STEPS.md`
- **未実装事項**: `docs/status/active/UNIMPLEMENTED_REPORT.md`
- **Phase 3計画**: `docs/planning/PHASE3_PACKAGING_PLAN.md`
- **リリース準備状況**: `docs/status/active/RELEASE_READINESS_20251128.md`

---

**更新日**: 2025-01-28  
**現在のフェーズ**: フェーズ1（即座の動作確保） - flm CLIバイナリのビルドと配置
