# Packaged CA Artifacts

This directory is a placeholder. The real `flm-ca.crt` is generated by the build pipeline
and copied here before bundling the installer.

Do not commit production certificates to the repository.

